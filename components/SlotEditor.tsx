// SlotEditor component replaced with a lightweight stub because the project
// centralizes slot initialization and mutations on the server. If you need to
// restore the editor UI, reintroduce an implementation that calls server APIs
// rather than writing directly from the client.

export default function SlotEditorStub(): null {
  return null;
}
